PORT=8080

# A strong, random string for signing JWTs
JWT_SECRET=your_super_secret_key_here_change_this_in_production

# A static API key for our future Lambda function to use
INTERNAL_API_KEY=a_very_secure_static_api_key_for_lambda_change_this

NODE_ENV=development
